node_modules/
dist/
dist-ssr/
*.local
.DS_Store
.vite/
coverage/

# Match data isn't checked in — drop CSVs in through the Data page instead.
*.csv

# Imported datasets written by the app (derived from Oracle's Elixir CSVs).
/data/
